export interface TransactionRepositoryI {
    // start(): Promise<TransactionRepositoryI>;
    run(): Promise<any>;

}

// export interface TransactionRepositoryI {
//     commit(): Promise<void>;
//     rollback(): Promise<void>;
// }
